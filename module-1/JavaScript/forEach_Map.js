let numbers = [1, 2, 3];

//forEach when we dont to create a new array
// numbers.forEach(number => console.log(number * 2));

//Map when we want to create a new array based on original array

// let mapValue = numbers.map((number) => {
//     return number * 2;
// });
// console.log(mapValue);

numbers.map(number =>
    //return number * 2;
    console.log(number * 2)

);
//console.log(mapValue);

